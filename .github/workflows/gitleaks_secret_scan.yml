name: Gitleaks scan

permissions:
  contents: read
  pull-requests: read

on:
  pull_request:
  workflow_dispatch:

jobs:
  scan:
    uses: ./.github/workflows/repository_gitleaks_secret_scan.yml@main
    secrets:
      NHSAPP_GITLEAKS_LICENSE: ${{ secrets.NHSAPP_GITLEAKS_LICENSE }}
